function [x, allx] = steepest(Grad, x, maxits, deltax)
    
    vecsize = size(x);
    allx = double(zeros(maxits, vecsize(2)));
    allx(1,:) = x;
    
    for i = 2:maxits
        
        gradient = Grad(x);
        if gradient == 0
            break
        end
        
        if norm(x-allx(i-1
        
        oldX = x;
        x = linesearch(Grad, x, -gradient);
        allx(i) = x;
        
    end
end
